{
  "name": "rw-bc-indexer",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "ponder dev",
    "start": "ponder start",
    "start:fresh": "node scripts/reset-schema.mjs && ponder start",
    "reset-schema": "node scripts/reset-schema.mjs",
    "codegen": "ponder codegen",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "db": "ponder db"
  },
  "dependencies": {
    "pg": "^8.21.0",
    "ponder": "0.16.6",
    "viem": "^2.52.2"
  },
  "devDependencies": {
    "@types/node": "^24",
    "@vitest/coverage-v8": "^4.1.9",
    "typescript": "^5",
    "vitest": "^4.1.9"
  }
}
